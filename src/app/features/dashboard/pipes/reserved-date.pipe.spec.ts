import { ReservedDatePipe } from './reserved-date.pipe';

describe('ReservedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ReservedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
