import { EnglishHijriDatePipe } from './english-hijri-date.pipe';

describe('EnglishHijriDatePipe', () => {
  it('create an instance', () => {
    const pipe = new EnglishHijriDatePipe();
    expect(pipe).toBeTruthy();
  });
});
