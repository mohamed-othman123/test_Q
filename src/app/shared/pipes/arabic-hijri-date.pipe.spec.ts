import { ArabicHijriDatePipe } from './arabic-hijri-date.pipe';

describe('ArabicHijriDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArabicHijriDatePipe();
    expect(pipe).toBeTruthy();
  });
});
