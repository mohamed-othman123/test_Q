import { ArabicMonthDatePipe } from './arabic-month-date.pipe';

describe('ArabicMonthDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArabicMonthDatePipe();
    expect(pipe).toBeTruthy();
  });
});
